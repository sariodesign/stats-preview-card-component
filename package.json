{
  "name": "stats-preview-card-component-main",
  "version": "1.0.0",
  "description": "![Design preview for the Stats preview card component coding challenge](./design/desktop-preview.jpg)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-scss": "node-sass -w ./scss/ -o ./dist/css/ --output-style compressed",
    "copy-file": "copyfiles index.html ./images/*.{png,jpg} ./dist",
    "build": "npm run compile-scss & npm run copy-file"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "copyfiles": "^2.4.1",
    "node-sass": "^6.0.0"
  }
}
